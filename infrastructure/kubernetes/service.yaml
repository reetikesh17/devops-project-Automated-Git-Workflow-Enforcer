apiVersion: v1
kind: Service
metadata:
  name: git-workflow-enforcer-service
spec:
  selector:
    app: git-workflow-enforcer
  ports:
  - protocol: TCP
    port: 80
    targetPort: 8080
  type: LoadBalancer
